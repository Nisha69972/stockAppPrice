server:
  port: 7001
  
spring:
   datasource:
      url: jdbc:mysql://localhost:3306/stocks_prices_db
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
   jpa:
      hibernate.ddl-auto: update
      properties:
         hibernate:
            dialect: org.hibernate.dialect.MySQL8Dialect
